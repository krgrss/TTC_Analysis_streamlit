"""TTC analysis package public API (curated).

This exposes the small set of stable, cross-module helpers the rest of the
project (API/dashboard/tests) can import without reaching into internals.
"""
